name: CI

on: [push]

jobs:
  build:

    runs-on: 10-alpine
    
    steps:
    - uses: actions/checkout@v1
    - name: Run a multi-line script
      run: |
        echo Starting nodejs server,
        node hello-world.js
